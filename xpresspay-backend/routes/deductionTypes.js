const express = require('express'); const router = express.Router(); const db = require('../db'); router.get('/', async (req, res) => { try { const [rows] = await db.query('SELECT * FROM deduction_types'); res.json(rows); } catch (err) { console.error(err); res.status(500).send('Server Error'); } }); router.post('/', async (req, res) => { try { const { code, label, method, rate, applies_to, is_mandatory, active } = req.body; const [result] = await db.query('INSERT INTO deduction_types (code, label, method, rate, applies_to, is_mandatory, active) VALUES (?, ?, ?, ?, ?, ?, ?)', [code, label, method, rate, applies_to, is_mandatory, active]); res.json({ id: result.insertId }); } catch (err) { console.error(err); res.status(500).send('Server Error'); } }); module.exports = router;
