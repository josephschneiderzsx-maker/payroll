const express = require('express'); const cors = require('cors'); const config = require('./config'); const db = require('./db'); const app = express(); const port = config.port || 3000; const employeesRouter = require('./routes/employees'); const departmentsRouter = require('./routes/departments'); const payrollPeriodsRouter = require('./routes/payrollPeriods'); const payrollRunsRouter = require('./routes/payrollRuns'); const deductionTypesRouter = require('./routes/deductionTypes'); const employeeDeductionsRouter = require('./routes/employeeDeductions'); const employeeIncomesRouter = require('./routes/employeeIncomes'); app.use(cors()); app.use(express.json()); app.use('/api/employees', employeesRouter); app.use('/api/departments', departmentsRouter); app.use('/api/payroll-periods', payrollPeriodsRouter); app.use('/api/payroll-runs', payrollRunsRouter); app.use('/api/deduction-types', deductionTypesRouter); app.use('/api/employee-deductions', employeeDeductionsRouter); app.use('/api/employee-incomes', employeeIncomesRouter); app.get('/', (req, res) => { res.send('Hello World!'); }); app.get('/api/db-test', async (req, res) => { try { const [rows] = await db.query('SELECT NOW()'); res.json(rows[0]); } catch (err) { console.error(err); res.status(500).send('Database connection test failed'); } }); app.listen(port, () => { console.log(`Server is running on port ${port}`); });
